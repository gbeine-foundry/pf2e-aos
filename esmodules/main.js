const moduleID = "pf2e-aos";

Hooks.once("init", () => {
    console.log("PF2E AoS | Aventurien on Steroids wird initialsiert.");
});

Hooks.on("ready", function() {
    console.log("PF2E AoS | Aventurien on Steroids wird konfiguriert.");
});
